
name: Deploy Avalon 
on:
  workflow_dispatch:
    inputs:
      host_machine:
        required: false
        type: string
        default: 'e7270-server'
      domain:
        required: false
        type: string
        default: 'avalon.james-fagg.uk'
      stack_name:
        required: false
        type: string
        default: 'avalon'
  workflow_call:
    inputs:
      host_machine:
        required: false
        type: string
        default: 'e7270-server'
      domain:
        required: false
        type: string
        default: 'avalon.james-fagg.uk'
      stack_name:
        required: false
        type: string
        default: 'avalon'
  push:
    branches:
      - main
      - '**'
    paths:
      - '.github/workflows/avalon-deploy.yml'
      - 'avalon/**'
env:
  host_machine: ${{ inputs.host_machine || 'e7270-server' }}
  domain: ${{ inputs.domain || 'avalon.james-fagg.uk' }}
  stack_name: ${{ inputs.stack_name || 'avalon' }}

jobs:
  database_create:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    concurrency:
      group: homelab-deployments
      cancel-in-progress: false
      queue: max
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Set node config
        id: node_config
        run: |
          if [ "${{ env.host_machine }}" = "rpi4-server" ]; then
            echo "ssh_host=${{ vars.RPI4_IP }}" >> $GITHUB_OUTPUT
            echo "ssh_user=${{ vars.RPI4_USER }}" >> $GITHUB_OUTPUT
            echo "ssh_key<<EOF_KEY" >> $GITHUB_OUTPUT
            echo "${{ secrets.SSH_KEY_RPI4 }}" >> $GITHUB_OUTPUT
            echo "EOF_KEY" >> $GITHUB_OUTPUT
          elif [ "${{ env.host_machine }}" = "e7270-server" ]; then
            echo "ssh_host=${{ vars.E7270_IP }}" >> $GITHUB_OUTPUT
            echo "ssh_user=${{ vars.E7270_USER }}" >> $GITHUB_OUTPUT
            echo "ssh_key<<EOF_KEY" >> $GITHUB_OUTPUT
            echo "${{ secrets.SSH_KEY_E7270 }}" >> $GITHUB_OUTPUT
            echo "EOF_KEY" >> $GITHUB_OUTPUT
          fi
     
      - name: Install WireGuard
        run: |
          sudo apt-get install -y wireguard || (sudo apt-get update && sudo apt-get install -y wireguard)  

      - name: Write WireGuard config
        run: |
          cat > wg0.conf << EOF
          [Interface]
          PrivateKey = ${{ secrets.WG_PRIVATE_KEY }}
          Address = 192.168.2.2/30
          DNS = ${{ vars.Keepalived_IP }}
          MTU = 1250

          [Peer]
          PublicKey = ${{ secrets.WG_PUBLIC_KEY }}
          PresharedKey = ${{ secrets.WG_PRESHARED_KEY }}
          AllowedIPs = 192.168.0.0/24, ${{ vars.IP_LJFCLOUD_ARM }}/32
          Endpoint = ${{ vars.WG_ENDPOINT }}:51820
          PersistentKeepalive = 25
          EOF

      - name: Bring up VPN on runner
        run: |
          success=false
          for i in 1 2 3 4 5; do
            if sudo wg-quick up ./wg0.conf; then
              success=true
              break
            fi
            echo "wg-quick up failed (attempt $i/5). Retrying in 5s..."
            sleep 5
          done
          if [ "$success" != true ]; then
            echo "::error::wg-quick up failed after 5 attempts"
            exit 1
          fi

          for i in 1 2 3 4 5; do
            if sudo wg show wg0 latest-handshakes | grep -qv '^\s*[a-zA-Z0-9+/=]*\s*0$'; then
              break
            fi
            echo "No WireGuard handshake yet (attempt $i/5). Waiting 2s..."
            sleep 2
          done
          sudo wg show

      - name: Override system DNS resolver on runner
        run: |
          sudo rm /etc/resolv.conf
          echo "nameserver 1.1.1.1" | sudo tee /etc/resolv.conf

      - name: Check postgres container exists
        id: check_postgres
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ steps.node_config.outputs.ssh_host }}
          username: ${{ steps.node_config.outputs.ssh_user }}
          key: ${{ steps.node_config.outputs.ssh_key }}
          capture_stdout: true
          script: |
            if docker inspect postgres >/dev/null 2>&1; then
              echo "POSTGRES_EXISTS=true"
            else
              echo "POSTGRES_EXISTS=false"
            fi

      # First-run bootstrap: if postgres has never been deployed to this host,
      # the steps below (pg_isready, psql database/user creation) would fail
      # against a container that doesn't exist yet. Trigger postgres-redeploy.yml
      # and wait for it before continuing.
      - name: Trigger postgres-redeploy if missing
        if: ${{ !contains(steps.check_postgres.outputs.stdout, 'POSTGRES_EXISTS=true') }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "postgres container not found on ${{ env.host_machine }} - triggering postgres-redeploy.yml"
          gh workflow run postgres-redeploy.yml --ref ${{ github.ref_name }}
          sleep 10
          RUN_ID=$(gh run list --workflow=postgres-redeploy.yml --branch ${{ github.ref_name }} --limit 1 --json databaseId -q '.[0].databaseId')
          echo "Waiting for postgres-redeploy run $RUN_ID to complete..."
          gh run watch "$RUN_ID" --exit-status

      - name: Check postgres is up
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ steps.node_config.outputs.ssh_host }}
          username: ${{ steps.node_config.outputs.ssh_user }}
          key: ${{ steps.node_config.outputs.ssh_key }}
          script: |
            sudo docker exec postgres pg_isready -U svc_gitopsdeploy

      - name: Create database and user if not exists
        uses: appleboy/ssh-action@v1.2.5
        env:
          POSTGRES_DEPLOYMENT_USER: svc_gitopsdeploy
          POSTGRES_DEPLOYMENT_USER_PASSWORD: ${{ secrets.svc_gitopsdeploy_password }}
          POSTGRES_APP_DB_NAME: avalon
          POSTGRES_APP_USER: svc_avalon
          POSTGRES_APP_PASSWORD: ${{ secrets.svc_avalon_password }}
        with:
          host: ${{ steps.node_config.outputs.ssh_host }}
          username: ${{ steps.node_config.outputs.ssh_user }}
          key: ${{ steps.node_config.outputs.ssh_key }}
          envs: POSTGRES_APP_USER,POSTGRES_APP_PASSWORD,POSTGRES_APP_DB_NAME
          script: |
            docker exec -i postgres psql -U svc_gitopsdeploy -d postgres_db -v ON_ERROR_STOP=1 \
              -v dbuser="$POSTGRES_APP_USER" \
              -v dbpass="$POSTGRES_APP_PASSWORD" \
              -v dbname="$POSTGRES_APP_DB_NAME" <<'SQL'
            SET createrole_self_grant = 'set,inherit';

            SELECT 'CREATE ROLE ' || quote_ident(:'dbuser') || ' WITH LOGIN PASSWORD ' || quote_literal(:'dbpass')
            WHERE NOT EXISTS (SELECT FROM pg_catalog.pg_roles WHERE rolname = :'dbuser')
            \gexec
            GRANT :"dbuser" TO CURRENT_USER WITH SET TRUE;
            
            SELECT 'CREATE DATABASE ' || quote_ident(:'dbname') || ' OWNER ' || quote_ident(:'dbuser')
            WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = :'dbname')
            \gexec
            SQL

      - name: Bring down VPN
        if: always()
        run: |
          sudo wg-quick down ./wg0.conf
          sudo wg show



  deploy:
    needs: database_create
    runs-on: ubuntu-latest
    concurrency:
      group: homelab-deployments
      cancel-in-progress: false
      queue: max
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Set node config
        id: node_config
        run: |
          if [ "${{ env.host_machine }}" = "rpi4-server" ]; then
            echo "ssh_host=${{ vars.RPI4_IP }}" >> $GITHUB_OUTPUT
            echo "ssh_user=${{ vars.RPI4_USER }}" >> $GITHUB_OUTPUT
            echo "ssh_key<<EOF_KEY" >> $GITHUB_OUTPUT
            echo "${{ secrets.SSH_KEY_RPI4 }}" >> $GITHUB_OUTPUT
            echo "EOF_KEY" >> $GITHUB_OUTPUT
          elif [ "${{ env.host_machine }}" = "e7270-server" ]; then
            echo "ssh_host=${{ vars.E7270_IP }}" >> $GITHUB_OUTPUT
            echo "ssh_user=${{ vars.E7270_USER }}" >> $GITHUB_OUTPUT
            echo "ssh_key<<EOF_KEY" >> $GITHUB_OUTPUT
            echo "${{ secrets.SSH_KEY_E7270 }}" >> $GITHUB_OUTPUT
            echo "EOF_KEY" >> $GITHUB_OUTPUT
          fi

      - name: Install WireGuard
        run: |
          sudo apt-get install -y wireguard || (sudo apt-get update && sudo apt-get install -y wireguard)            

      - name: Write WireGuard config
        run: |
          cat > wg0.conf << EOF
          [Interface]
          PrivateKey = ${{ secrets.WG_PRIVATE_KEY }}
          Address = 192.168.2.2/30
          DNS = ${{ vars.Keepalived_IP }}
          MTU = 1250

          [Peer]
          PublicKey = ${{ secrets.WG_PUBLIC_KEY }}
          PresharedKey = ${{ secrets.WG_PRESHARED_KEY }}
          AllowedIPs = 192.168.0.0/24, ${{ vars.IP_LJFCLOUD_ARM }}/32
          Endpoint = ${{ vars.WG_ENDPOINT }}:51820
          PersistentKeepalive = 25
          EOF

      - name: Bring up VPN
        run: |
          for i in $(seq 1 5); do
            echo "Attempt $i/5: bringing up WireGuard..."
            if sudo wg-quick up ./wg0.conf; then
              echo "WireGuard up successfully"
              break
            fi
            if [ $i -eq 5 ]; then
              echo "WireGuard failed after 5 attempts"
              exit 1
            fi
            echo "Failed, waiting 10s before retry..."
            sudo ip link delete dev wg0 2>/dev/null || true
            sleep 10
          done
          sudo wg show

      - name: Authenticate to Portainer
        id: auth
        run: |
          RESPONSE=$(curl -X POST "http://${{ vars.RPI4_IP }}:83/api/auth" \
            -H "Content-Type: application/json" \
            -d '{
                 "Username":"${{ vars.APP_USER }}",
                 "Password":"${{ secrets.APP_PASSWORD }}"
                 }')
          TOKEN=$(echo "$RESPONSE" | jq -r '.jwt')
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Get Portainer EndpointId
        id: endpoint
        env:
          host_machine: ${{ env.host_machine || 'ljfcloud-arm' }}
        run: |
          echo "Looking for endpoint with name: $host_machine"
          RESPONSE=$(curl -s -X GET "http://${{ vars.RPI4_IP }}:83/api/endpoints" \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}")
          ENDPOINT_ID=$(echo "$RESPONSE" | jq -r --arg host_machine "$host_machine" '.[] | select(.Name==$host_machine) | .Id')
          echo "endpoint_id=$ENDPOINT_ID" >> $GITHUB_OUTPUT

      - name: Get Stack ID (if exists)
        id: get_stack_id
        run: |
          STACKS=$(curl --fail -s -X GET "http://${{ vars.RPI4_IP }}:83/api/stacks" \
          -H "Authorization: Bearer ${{ steps.auth.outputs.token }}" || echo "[]")
 
          # Filter by both Name AND EndpointId
          STACK_ID=$(echo "$STACKS" | jq -r \
            --arg name "${{ env.stack_name }}" \
            --argjson endpoint_id "${{ steps.endpoint.outputs.endpoint_id }}" \
            '.[] | select(.Name==$name and .EndpointId==$endpoint_id) | .Id')

          if [ -z "$STACK_ID" ] || [ "$STACK_ID" = "null" ]; then
            echo "Stack not found for this endpoint."
            echo "stack_id=0" >> $GITHUB_OUTPUT
          else
            echo "Stack found with ID: $STACK_ID"
            echo "stack_id=$STACK_ID" >> $GITHUB_OUTPUT
          fi

      - name: Get stack webhook token
        if: ${{ steps.get_stack_id.outputs.stack_id != '0' }}
        id: get_webhook
        run: |
          WEBHOOK_TOKEN=$(curl -s \
            --max-time 10 \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}" \
            "http://192.168.0.2:83/api/stacks/${{ steps.get_stack_id.outputs.stack_id }}?endpointId=${{ steps.endpoint.outputs.endpoint_id }}" \
            | jq -r '.AutoUpdate.Webhook')

          echo "Webhook token: $WEBHOOK_TOKEN"
          echo "webhook_token=$WEBHOOK_TOKEN" >> $GITHUB_OUTPUT

      - name: Generate UUID
        if: ${{ steps.get_stack_id.outputs.stack_id == '0' }}
        id: uuid
        run: |
          echo "uuid=$(uuidgen)" >> $GITHUB_OUTPUT

      - name: Create Stack from GitHub
        if: ${{ steps.get_stack_id.outputs.stack_id == '0' }}
        run: |
          OCI_KEY_BASE64=$(echo "${{ secrets.NETWORKUPDATERSA_API_KEY }}" | base64 -w 0)

          echo "Creating stack from GitHub..."
          RESPONSE=$(curl -v -s -S -X POST "http://${{ vars.RPI4_IP }}:83/api/stacks/create/standalone/repository?endpointId=${{ steps.endpoint.outputs.endpoint_id }}" \
          -H "Authorization: Bearer ${{ steps.auth.outputs.token }}" \
          -H "Content-Type: application/json" \
          -d '{
               "Name": "${{ env.stack_name }}",
               "RepositoryURL": "https://github.com/liamj-f/homelab-setup",
               "RepositoryReferenceName": "${{ github.ref }}",
               "ComposeFile": "${{ env.stack_name }}/${{ env.stack_name }}-compose.yml",
               "RepositoryAuthentication": false,
               "AutoUpdate": {
                              "forcePullImage": true,
                              "forceUpdate": true,
                              "Webhook": "${{ steps.uuid.outputs.uuid }}"                           
                             },
                              "Env": [
                                    {"name":"POSTGRES_DB",         "value":"avalon"},
                                    {"name":"POSTGRES_USER",       "value":"svc_avalon"},
                                    {"name":"POSTGRES_PASSWORD",   "value":"${{ secrets.svc_avalon_password }}"}
                                    ],
               "Prune": true,
               "StackFileVersion": "3"
              }'
                    )

            echo "API Response: $RESPONSE"
            echo "stack_response=$RESPONSE" >> $GITHUB_OUTPUT

      - name: Update Stack Environment Variables
        if: ${{ steps.get_stack_id.outputs.stack_id != '0' }}
        run: |
            OCI_KEY_BASE64=$(echo "${{ secrets.NETWORKUPDATERSA_API_KEY }}" | base64 -w 0)

            RESPONSE=$(curl -s -i -v --max-time 300 -X PUT "http://${{ vars.RPI4_IP }}:83/api/stacks/${{ steps.get_stack_id.outputs.stack_id }}/git/redeploy?endpointId=${{ steps.endpoint.outputs.endpoint_id }}" \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}" \
            -H "Content-Type: application/json" \
            -d '{
                  "AutoUpdate": {
                          "forcePullImage": true,
                          "forceUpdate": true,
                          "Webhook": "${{ steps.get_webhook.outputs.webhook_token }}"
                          },
                  "PullImage": false,
                  "RepositoryReferenceName": "${{ github.ref }}",
                  "repositoryAuthentication": false,
                              "Env": [
                                    {"name":"POSTGRES_DB",         "value":"avalon"},
                                    {"name":"POSTGRES_USER",       "value":"svc_avalon"},
                                    {"name":"POSTGRES_PASSWORD",   "value":"${{ secrets.svc_avalon_password }}"}
                                    ],
                  "Prune": false
                }'  )

              echo "API Response: $RESPONSE" 

#      - name: Redeploy Stack using webhook
#        if: ${{ steps.get_stack_id.outputs.stack_id != '0' }}
#        run: |
#          RESPONSE=$(curl -s -i -v --max-time 300 -X POST "http://${{ vars.RPI4_IP }}:83/api/stacks/webhooks/${{ steps.get_webhook.outputs.webhook_token }}?pullimage=true" \
#          -H "Authorization: Bearer ${{ steps.auth.outputs.token }}" )
#
#          echo "API Response: $RESPONSE"



      - name: Bring down VPN
        if: always()
        run: |
          sudo wg-quick down ./wg0.conf
          sudo wg show

  proxy_setup_onprem:
    uses: ./.github/workflows/proxy_config_onprem.yml
    secrets: inherit
    with:
      domain: ${{ inputs.domain || 'avalon.james-fagg.uk' }}
      app_host_machine: ${{ inputs.host_machine || 'e7270-server' }}
      forward_port: 92
  proxy_setup_oci:
    uses: ./.github/workflows/proxy_config_oci.yml
    secrets: inherit
    with:
      domain: ${{ inputs.domain || 'avalon.james-fagg.uk' }}
      host_machine: ${{ inputs.host_machine || 'e7270-server' }}

  f2b:
    needs: deploy
    uses: ./.github/workflows/fail2ban-jail-deploy.yml
    # fail2ban-jail-deploy.yml's own job requests actions:write (to trigger
    # fail2ban-redeploy.yml on first-run bootstrap) - must be granted
    # explicitly here or the call is rejected regardless of repo defaults.
    permissions:
      actions: write
      contents: read
    secrets: inherit
    with:
      host_machine: ${{ inputs.host_machine || 'e7270-server' }}
      stack_name: ${{ inputs.stack_name || 'avalon' }}
      log_filename: access.log
