---
theme: 'dark'

server:
  address: 'tcp://:9091/'

log:
  level: 'info'
  format: 'text'
  file_path: '/config/log/authelia.log'
  keep_stdout: true

totp:
  issuer: 'james-fagg.uk'

authentication_backend:
  file:
    path: '/config/users_database.yml'
    password:
      algorithm: 'argon2'

access_control:
  # No backends are wired up to Authelia's forward-auth yet - this just protects
  # the portal itself. Add rules here when other services start using it.
  default_policy: 'two_factor'

session:
  secret: '${AUTHELIA_SESSION_SECRET}'
  cookies:
    - name: 'authelia_session'
      domain: 'james-fagg.uk'
      authelia_url: 'https://auth.14monarch.james-fagg.uk'
      same_site: 'lax'
      expiration: '1 hour'
      inactivity: '5 minutes'
      remember_me: '1 month'
  redis:
    host: 'authelia_valkey'
    port: 6379

regulation:
  modes:
    - 'user'
  max_retries: 3
  find_time: '2 minutes'
  ban_time: '5 minutes'

storage:
  encryption_key: '${AUTHELIA_STORAGE_ENCRYPTION_KEY}'
  local:
    path: '/config/db.sqlite3'

notifier:
  disable_startup_check: false
  smtp:
    address: 'submissions://smtp.gmail.com:465'
    username: '${gmail_address}'
    password: '${gmail_app_password}'
    sender: 'Authelia <${AUTHELIA_SMTP_SENDER}>'

identity_validation:
  reset_password:
    jwt_secret: '${AUTHELIA_JWT_SECRET}'
